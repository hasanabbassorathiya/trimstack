node_modules/
dist/
coverage/
*.tsbuildinfo

# Build-time demo snapshot (regenerated by scripts/build-demo-data.mjs / CI)
web/public/demo/

# Local databases (runtime state, never committed)
server/data/*.sqlite
server/data/*.sqlite-journal
server/data/*.sqlite-wal
server/data/*.sqlite-shm

# QA evidence (regenerated per capture run; archived with the phase that certifies it)
qa-screenshots/
test-results/
playwright-report/

# Env & OS
.env
*.log
.DS_Store
